$Id$

Code out of cvs doesn't contain
Makefile.in's and configure, soo
you need to run

./autogen.sh

to initialize your tree.

recommended .cvsrc:

diff -u
history -e -z LT -a
status -v
cvs -z3
checkout -A
log -N
update -d -P

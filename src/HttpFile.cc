#include "HttpFile.hh"

HttpFile::HttpFile(const std::string& uri)
{
}

HttpFile::~HttpFile()
{
}

void HttpFile::fetch()
{
	assert(false);
}

byte* HttpFile::getData(size_t& len)
{
	assert(false);
}
